import * as assert from 'assert';

import * as vscode from 'vscode';
import User from "../../class/user";


suite('Auth Test Suite', () => {

    /*
    test('Check is user signed-in', () => {
        const user = new User();
        return user.signIn();
    });
     */

    test('User must sign-in', () => {

    });
});
